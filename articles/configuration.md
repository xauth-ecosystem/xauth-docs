# Configuration
Detailed guide on configuring XAuth Core and other modules.

## XAuth Core Configuration
The main configuration file is `config.yml`.

## Database Settings
Supports SQLite, MySQL, and MariaDB.
